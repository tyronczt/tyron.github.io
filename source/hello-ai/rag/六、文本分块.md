# 六、文本分块

搭 RAG 系统，数据加载搞定了，下一步就是把文档切成小块。

这件事听起来简单，找个分词器按字数切就完了对吧？但真正做过的人都知道，分块策略选错了，后面检索怎么调都不对。召回的内容不是缺胳膊少腿，就是混入一堆不相关的东西，模型想答对都难。

我踩过这个坑。第一次做 RAG 的时候，觉得分块有什么好研究的，不就是切切切吗。结果上线一测，用户问"公司年假怎么算"，AI 召回的内容刚好从"年假"这个词中间切断了，答得驴唇不对马嘴。

从那以后我就知道了：分块这件事，值得认真对待。

![Gemini_Generated_Image_eybuwxeybuwxeybu.png](imgs/chunking/Gemini_Generated_Image_eybuwxeybuwxeybu.png)
## 什么是文本分块？

文本分块（Text Chunking）就是把加载后的长文档切成更小、更易于处理的单元。每个小块，是后面向量检索和模型处理的基本单位。

你可能会问：为什么不直接把整篇文档向量化存进去？

有三个原因。

**第一个，大模型有上下文窗口限制。** 你的文档可能有 10 万字，但模型的上下文窗口可能是 128K tokens，直接塞进去会被截断，后面的内容模型根本看不到。

**第二个，检索效率。** 让 AI 在一整篇长文档里找几句关键信息，计算量巨大，响应慢，而且信息被稀释，答案不精准。切成小块之后，每个小块单独算向量，检索的时候直接在向量空间里做相似度搜索，快得多，精准得多。

**第三个，上下文语义完整性。** 糟糕的分块就像用尺子把"我喜欢吃苹果"机械切成"我喜欢吃苹"和"果"——AI 看了根本不知道在说什么。好的分块策略会遵循语义边界，确保每个块内部信息完整。

![rag-text-chunking.png](imgs/chunking/rag-text-chunking.png)

## 两个核心参数：chunkSize 和 overlap

动手切之前，有两个参数必须搞清楚：`chunkSize`（块大小）和 `overlap`（重叠量）。
![chunking_concept.png](imgs/chunking/chunking_concept.png)

### chunkSize 怎么理解

chunkSize 就是每个块的长度上限。比如你设 chunkSize = 200，意思是每个块最多包含 200 个字符或者 200 个 token。

这块设多大合适？没有标准答案，但有一个基本的权衡：

块太大，每个块包含的信息多，但检索时容易混入不相关的内容，精度下降。就像用户问退货政策，结果返回了一整章包含退货、换货、维修的内容，模型还得自己从里面挑。

块太小，每个块很精准，但可能把一个完整的意思切断了，上下文丢失。就像把一条退货规则从中间劈开，前半句和后半句单独看都不知道在说什么。

一般来说，200 到 1000 个字符是比较常见的范围，具体用多少，还是得根据你的文档类型和检索需求来反复调。

### overlap 是什么，为什么需要它

overlap 是指相邻两个块之间共享的文本长度。

打个比方：你在看一本小说，每次只能记住一页的内容。如果你严格按页翻，第 1 页看完翻到第 2 页，那第 1 页最后一句话和第 2 页第一句话之间的联系就断了。但如果你每次翻页时，把上一页最后几行重新看一遍，这几行就是重叠的部分，它帮你保持了上下文的连贯性。

用一个具体的例子来看。假设知识库里有这样一段退货政策：

> 自签收之日起 7 天内，商品未经使用且不影响二次销售的，消费者可申请七天无理由退货。生鲜食品、定制商品、贴身衣物等特殊品类不适用此规则，具体以商品详情页标注为准。

如果 chunkSize = 40，不加 overlap，切出来可能是：

- 块 1：`自签收之日起 7 天内，商品未经使用且不影响二次销售的，消费者可申请七天`
- 块 2：`无理由退货。生鲜食品、定制商品、贴身衣物等特殊品类不适用此规则，具体`
- 块 3：`以商品详情页标注为准。`

注意看，"七天无理由退货"这个关键词被切成了两半，分别落在块 1 和块 2 里。用户搜"七天无理由退货"，两个块都匹配不完整。

加上 overlap = 15 之后，切出来变成：

- 块 1：`自签收之日起 7 天内，商品未经使用且不影响二次销售的，消费者可申请七天`
- 块 2：`消费者可申请七天无理由退货。生鲜食品、定制商品、贴身衣物等特殊品类不适`
- 块 3：`特殊品类不适用此规则，具体以商品详情页标注为准。`

块 2 的开头和块 1 的结尾有重叠，"七天无理由退货"在块 2 里是完整的了。

当然，overlap 也不是越大越好。overlap 太大意味着大量重复文本，存储和计算成本都会上升。一般经验是 overlap 设为 chunkSize 的 10% 到 25%。

### 用什么单位：字符 vs token

你可能注意到了，前面说 chunkSize 的时候，有时说字符，有时说 token。这两个东西不一样，有必要区分一下。

字符（Character）就是你肉眼看到的每一个符号。"你好"是 2 个字符，"Hello" 是 5 个字符，空格和标点也各算 1 个字符。

Token 是大模型实际处理文本的最小单位。大模型不是一个字一个字地读文本的，它会先把文本切成一个个 token。对于英文，一个常见单词通常是 1 个 token，长一点的单词可能被拆成 2-3 个 token。对于中文，一个汉字通常是 1-2 个 token。


| 文本           | 字符数 | 大约 token 数 |
| -------------- | ------ | ------------- |
| 七天无理由退货 | 7      | 7-10          |
| Hello World    | 11     | 2             |
| 退货政策       | 4      | 4-6           |

为什么要关心这个？因为大模型的上下文窗口是按 token 算的，不是按字符算的。如果你用字符数来设 chunkSize，实际消耗的 token 数可能比你预期的多，尤其是中文场景。

不过在入门阶段，用字符数来设 chunkSize 完全够用。等你对 token 有了更深的理解，再考虑切换到基于 token 的分块也不迟。

## 分块策略：怎么切，切多碎

根据文档类型和任务选择合适策略是成功关键，以下是几种主流策略：


| 策略类型                                     | 核心原理                                                                   | 优点                                                 | 缺点                                                         |
| -------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------ |
| **固定大小分块-Fixed Size Chunking**         | 按预设的字符数或 Token 数机械切分。                                        | 简单、速度快、可预测。                               | 易破坏语义完整性。                                           |
| **重叠分块-Overlapping Chunking**            | 相邻块之间保留一定比例（如 10%-25%）的重复内容，以保持上下文连贯。         | 有效减少边界信息丢失，提升检索召回率。               | 存储成本增加，块之间存在冗余内容。                           |
| **递归分块-Recursive Chunking**              | 按优先级顺序（段落→句子→空格→字符）递归切分，直至块大小符合要求。       | 在满足大小限制的同时，最大限度保留文档逻辑结构。     | 实现稍复杂，有一定性能开销。                                 |
| **语义分块-Semantic Chunking**               | 计算句子 Embedding 的相似度，在主题切换处（相似度下降）切分。              | 语义高度连贯，检索质量高。                           | 计算成本高，速度慢。                                         |
| **滑动窗口分块-Sliding Window Chunking**     | 通过滑动窗口创建重叠块，窗口每次滑动固定步长，确保相邻块共享内容。         | 保持跨部分上下文连贯，避免边界信息丢失，实现简单。   | 块间冗余度高，存储成本增加，可能产生过多相似块影响检索精度。 |
| **模式特定分块-Content-Specific Chunking**   | 按内容类型（如文本、表格、代码、图像）分别处理，每个类型按其性质分块。     | 针对不同内容类型优化，保留特定格式的结构和语义。     | 需要预先识别内容类型，实现复杂，通用性较弱。                 |
| **上下文增强分块-Context-Enriched Chunking** | 为每个块添加周围块的摘要或元数据（如标题、章节信息），保持序列间的上下文。 | 增强块的上下文信息，提升检索和理解质量，适合长文档。 | 元数据生成增加计算成本，块大小难以控制，可能超出模型限制。   |
| **子文档分块-Sub-document Chunking**         | 先总结整个文档或大章节，将摘要作为元数据附加到各个子块，适合大型文档集。   | 保留宏观语义结构，块间关联性强，便于文档级检索。     | 需要额外的摘要生成步骤，实现复杂，存储和计算成本较高。       |
| **基于文档结构分块-Document Based Chunking** | 利用标题、表格、页码等文档自身结构进行切分。                               | 贴合文档逻辑结构，信息组织性强。                     | 依赖高质量文档解析，通用性弱。                               |
| **智能体 (Agentic) 分块-LLM-based Chunking** | 由 Agent 根据任务（如总结、问答）动态决定如何切分。                        | 灵活性和针对性强。                                   | 实现复杂，目前不普及。                                       |
| **混合分块-Hybrid Chunking**                 | 结合多种分块策略，如 Token 分块 + 语义相似度合并。                         | 综合多种策略优势，兼顾效率与语义完整性。             | 实现复杂度较高，参数调优复杂。                               |

## 5. 文本分块示例

> chunk在线地址：https://chunkviz.up.railway.app/

### 固定大小分块

最简单粗暴的策略。按预设的字符数或 token 数把文档机械切分，不管你这一刀切在段落中间还是句子中间，到了字数就切。

这既是它的优点，也是它的缺点。优点是实现极其简单，性能好，不需要任何 NLP 处理，调试起来也容易。缺点是忽略文本结构，容易把句子、段落从中间切断，导致语义不完整。


| 维度   | 说明                                                                               |
| ------ | ---------------------------------------------------------------------------------- |
| 优点   | 实现极其简单，性能好，不需要任何 NLP 处理                                          |
| 缺点   | 完全忽略文本结构，容易把句子、段落从中间切断，导致语义不完整                       |
| 适合   | 文本结构不重要的场景，比如日志文件、纯数据文本；或者作为其他策略的兜底方案         |
| 不适合 | 有明确段落结构的文档（知识库、产品手册、政策文件），因为切断语义会严重影响检索质量 |

固定大小分块一般不用作主力策略，但经常作为保底方案——其他策略切完剩下太小的块，用固定大小来兜底。

代码示例：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/FixedSizeChunkingStrategy.java

执行结果：
![FixedSizeChunkingStrategy.png](imgs/chunking/FixedSizeChunkingStrategy.png)

### 重叠分块

重叠分块是在固定大小分块的基础上，让相邻的块之间共享一部分内容。这种策略的核心目的是解决边界信息丢失的问题。

假设你设置 chunkSize = 500，overlap = 100，那么：

- 块 1：包含文档的第 1-500 个字符
- 块 2：包含文档的第 401-900 个字符（与块 1 重叠 100 个字符）
- 块 3：包含文档的第 801-1300 个字符（与块 2 重叠 100 个字符）

重叠分块的工作原理：

```
文档：[ABCDEFGHIJKLMNOPQRSTUVWXYZ...]

固定大小分块（chunkSize=10）：
块1: [ABCDEFGHIJ]
块2: [KLMNOPQRST]
块3: [UVWXYZ...]

重叠分块（chunkSize=10, overlap=3）：
块1: [ABCDEFGHIJ]
块2: [HIJKLMNOP]  ← 与块1重叠 "HIJ"
块3: [PQRSTUVWXYZ]  ← 与块2重叠 "PQR"
```


| 维度     | 说明                                                                             |
| -------- | -------------------------------------------------------------------------------- |
| 优点     | 有效减少边界信息丢失，提升检索召回率；实现简单，只需在固定分块基础上添加偏移量   |
| 缺点     | 存储成本增加（约增加 overlap/chunkSize 的比例）；块间存在冗余内容，可能引入噪声  |
| 适合     | 需要高召回率的场景，如法律条文检索、医疗诊断知识库；连续性强、上下文依赖重的文本 |
| 不适合   | 存储资源极度受限的场景；文本块独立性要求高、重复内容会影响检索精度的场景         |
| 参数建议 | overlap 通常设为 chunkSize 的 10%-25%，过小的 overlap 效果不明显，过大则成本激增 |

重叠分块也常用，尤其是法律文档和医疗文档这类上下文连续性要求高的场景。成本贵一点，但召回质量确实好。

代码示例：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/OverlappingChunkingStrategy.java
执行结果：
![OverlappingChunkingStrategy.png](imgs/chunking/OverlappingChunkingStrategy.png)

### 递归分块（常用）

递归分块是目前实践中常用的策略。

它的思路一句话就能说清楚：先尝试用最大的分隔符切，切完如果某个块还是太大，就换一个更小的分隔符继续切，直到所有块都在 chunkSize 以内。

具体来说，它维护一个分隔符列表，按优先级从高到低排列：段落 → 句子 → 换行符 → 空格 → 字符。这个先粗后细的过程就是递归的含义——不是一刀切到底，而是逐层细化。

![recursive-chunking.png](imgs/chunking/recursive-chunking.png)

为什么这种方式好？因为它尽最大努力保留文本的结构。能按段落切就按段落切，段落太长了才按句子切，句子还太长才按逗号切……只有在万不得已的时候才会像固定大小分块那样按字符硬切。


| 维度   | 说明                                                                                |
| ------ | ----------------------------------------------------------------------------------- |
| 优点   | 兼顾了语义完整性和块大小控制，是目前最通用的分块策略                                |
| 缺点   | 分隔符列表需要根据语言调整（中文和英文的标点不同）；依赖文本中存在合理的分隔符      |
| 适合   | 绝大多数场景，尤其是你不确定该用什么策略的时候，递归分块是最安全的默认选择          |
| 不适合 | 对分块有特殊要求的场景，比如代码文件（需要按函数/类来切）、表格数据（需要按行来切） |

示例代码：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/RecursiveChunkingStrategy.java

执行结果：
![RecursiveChunkingStrategy.png](imgs/chunking/RecursiveChunkingStrategy.png)

如果你不知道该用什么策略，先用递归分块，不会错。基本都是先用递归分块跑通链路，看到具体问题之后，再根据文档特征换别的策略。

### 语义分块

前面几种策略有一个共同的局限：它们都是基于规则的——要么按字数切，要么按标点符号切。它们不理解文本在说什么。

语义分块换了一个完全不同的思路：用 Embedding 模型来判断文本的语义相似度，在语义发生明显变化的地方切割。

具体过程是这样的：

1. 先把文本按句子拆开（这一步可以简单地按句号切）
2. 对每个句子生成一个向量（Embedding）
3. 计算相邻句子之间的向量相似度
4. 当相邻句子的相似度低于某个阈值时，说明话题发生了转换，在这里切一刀

![semantic-chunking.png](imgs/chunking/semantic-chunking.png)

这种方式的好处是显而易见的：切出来的每个块在语义上是高度内聚的，不会出现一个块里混着两个不相关话题的情况。

更直接的做法是直接让大模型来判断分块边界。把文本交给大模型，让它找出主题切换的位置：

```
提示：请分析以下文本，在主题转换的地方标记 [CUT]。
文本：...
```


| 维度   | 说明                                                                                               |
| ------ | -------------------------------------------------------------------------------------------------- |
| 优点   | 切割点基于语义而非规则，分块质量最高，每个块的主题高度内聚                                         |
| 缺点   | 需要调用 Embedding 或者 Chat 模型，有额外的计算成本和延迟；阈值需要调参；对模型的质量有依赖        |
| 适合   | 对检索精度要求很高的场景，比如法律文档问答、医疗知识库、金融合规文档                               |
| 不适合 | 文档量特别大且对延迟敏感的场景；文本本身结构已经很清晰的场景（用递归分块就够了，没必要上语义分块） |

语义分块效果好，但成本也高。我一般是在核心文档上用，辅助性的长尾文档还是用递归分块。

示例代码：
https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/SemanticChunkingStrategy.java

执行结果：
![SemanticChunkingStrategy.png](imgs/chunking/SemanticChunkingStrategy.png)

### 滑动窗口分块

滑动窗口分块是一种特殊的重叠分块策略，通过固定步长的滑动窗口来创建文本块。

滑动窗口分块有三个核心参数：

- window_size：窗口大小（每个块的长度）
- stride：步长（窗口每次滑动的距离）
- overlap = window_size - stride：重叠长度

```
文档：[ABCDEFGHIJKLMNOPQRSTUVWXYZ...]

滑动窗口分块（window_size=8, stride=3）：
块1: [ABCDEFGH]
块2: [DEFGHIJK]  ← 向右滑动3个字符
块3: [GHIJKLMN]  ← 再向右滑动3个字符
块4: [JKLMNOPQ]
...
```


| 对比维度 | 普通重叠分块                        | 滑动窗口分块                           |
| -------- | ----------------------------------- | -------------------------------------- |
| 切分方式 | 按固定大小切分，相邻块共享尾部/头部 | 窗口平滑滑动，每个新块都是原窗口的平移 |
| 重叠控制 | 仅首尾重叠                          | 整个窗口范围内都有不同程度的重叠       |
| 块数量   | 相对较少                            | 相对较多（取决于 stride 大小）         |
| 适用场景 | 一般文档检索                        | 需要细粒度上下文保持的序列数据         |


| 维度     | 说明                                                                                            |
| -------- | ----------------------------------------------------------------------------------------------- |
| 优点     | 保持跨部分上下文连贯，避免边界信息丢失；实现简单直观；适合序列型数据                            |
| 缺点     | 块间冗余度高，存储成本显著增加；可能产生过多相似块，影响检索精度和效率；stride 参数需要仔细调优 |
| 适合     | 时间序列文本（聊天记录、日志）；需要细粒度上下文分析的代码；流式数据处理                        |
| 不适合   | 存储资源受限的场景；需要高检索精度的场景（大量相似块会干扰排序）；独立性强的文档集合            |
| 参数建议 | stride 通常设为 window_size 的 50%-75%，较小的 stride 产生更多块但上下文更完整                  |

滑动窗口分块我主要用在聊天记录和代码分析这类连续性要求高的场景，普通文档不太用。
示例代码：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/SlidingWindowChunkingStrategy.java
执行结果：
![SlidingWindowChunkingStrategy.png](imgs/chunking/SlidingWindowChunkingStrategy.png)

### 模式特定分块

模式特定分块的核心思想是：不同类型的内容应该采用不同的分块策略。文本、表格、代码、图像各有其最佳的分块方式。

考虑一个包含多种内容的 PDF 文档：正文段落适合按段落或句子切分，数据表格应该按行或按表格整体切分（不能把一行数据切开），代码块应该按函数、类或逻辑块切分。如果用统一的分块策略处理，会破坏这些内容的内在结构。


| 内容类型 | 分块策略             | 示例                              |
| -------- | -------------------- | --------------------------------- |
| 普通文本 | 递归分块或语义分块   | 按段落→句子层级切分              |
| Markdown | 按标题层级切分       | #、##、### 作为分隔符             |
| HTML     | 按标签结构切分       | `<div>`、`<section>`、`<p>` 等    |
| 表格     | 按行或按表切分       | 每行作为独立块，或整个表格+描述   |
| 代码     | 按函数/类/逻辑块切分 | AST 解析后按语法结构切分          |
| JSON/XML | 按节点切分           | 每个对象或数组元素作为块          |
| LaTeX    | 按章节和环境切分     | `\section{}`、`\begin{}...\end{}` |

代码分块需要特别说一下。用普通文本分块处理代码会把函数从中间切断，正确做法是用 AST（抽象语法树）解析后按函数完整切分：

```python
# 原始代码
def calculate_total(price, quantity):
    """计算总价"""
    discount = 0.9 if quantity > 10 else 1.0
    return price * quantity * discount

def apply_tax(amount, tax_rate):
    """应用税率"""
    return amount * (1 + tax_rate)
```

使用 AST 解析后，可以按函数完整切分：

- 块 1：完整的 `calculate_total` 函数
- 块 2：完整的 `apply_tax` 函数


| 维度   | 说明                                                                             |
| ------ | -------------------------------------------------------------------------------- |
| 优点   | 针对不同内容类型优化，保留特定格式的结构和语义；分块质量最高，专业文档处理效果好 |
| 缺点   | 需要预先识别内容类型，增加预处理复杂度；实现复杂，需要多种解析器；通用性较弱     |
| 适合   | 结构化文档（PDF、Word、HTML）；混合内容文档；代码仓库检索；技术文档和 API 文档   |
| 不适合 | 纯文本场景（增加不必要的复杂度）；快速原型验证；内容类型未知的文档流             |

### 上下文增强分块

上下文增强分块是一种"元数据增强"策略。它不为每个块添加周围块的内容，而是添加周围块的摘要或关键元数据，从而在控制块大小的同时保持上下文关联。

普通分块：

```
块内容：[产品的保修期为一年，自购买之日起计算。]
```

上下文增强后：

```
[章节：售后服务政策]
[前文摘要：本文档说明产品的退换货和保修政策]
块内容：[产品的保修期为一年，自购买之日起计算。]
[后文摘要：以下情况不在保修范围内...]
```


| 元数据类型   | 说明             | 示例                           |
| ------------ | ---------------- | ------------------------------ |
| 文档级元数据 | 整个文档的信息   | 标题、作者、创建时间、文档类型 |
| 章节级元数据 | 当前章节的信息   | 章节标题、层级、章节摘要       |
| 相邻块摘要   | 前后块的简短描述 | 前文讲了什么、后文将讲什么     |
| 关键词标签   | 块的核心主题     | #保修政策 #售后服务            |

实现方式有三种：预处理方式（在分块前提取文档结构）、后处理方式（分块后用 LLM 为每个块生成上下文摘要）、混合方式（结构元数据加生成式摘要）。


| 维度     | 说明                                                                                         |
| -------- | -------------------------------------------------------------------------------------------- |
| 优点     | 增强块的上下文信息，提升检索和理解质量；块间关联性强，便于追踪信息来源；适合长文档和复杂查询 |
| 缺点     | 元数据生成增加计算成本；增强后的块大小难以控制；需要额外的存储空间                           |
| 适合     | 长文档检索（论文、报告、书籍）；需要溯源的场景；复杂多跳问答；法律、医疗等专业领域           |
| 不适合   | 短文档或片段文本；对延迟极度敏感的场景；存储成本敏感的场景                                   |
| 最佳实践 | 元数据应简洁明了，控制在块大小的 20%-30%                                                     |

示例代码：
https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/ContextEnhancedChunkingStrategy.java
执行结果：![ContextEnrichedChunkingStrategy.png](imgs/chunking/ContextEnrichedChunkingStrategy.png)

### 子文档分块

子文档分块是一种"分层摘要"策略。它先对整个文档或大章节生成摘要，然后将这些摘要作为元数据附加到各个子块上。这样每个子块都能"感知"到它所在的更大上下文。

```
文档级摘要
├── 章节1摘要
│   ├── 块1.1 (内容 + 章节1摘要 + 文档级摘要)
│   ├── 块1.2 (内容 + 章节1摘要 + 文档级摘要)
│   └── 块1.3 (内容 + 章节1摘要 + 文档级摘要)
├── 章节2摘要
│   ├── 块2.1 (内容 + 章节2摘要 + 文档级摘要)
│   └── 块2.2 (内容 + 章节2摘要 + 文档级摘要)
└── 章节3摘要
    └── ...
```


| 维度   | 说明                                                                                 |
| ------ | ------------------------------------------------------------------------------------ |
| 优点   | 保留宏观语义结构，块间关联性强；便于文档级检索和跨块推理；减少"只见树木不见森林"     |
| 缺点   | 需要额外的摘要生成步骤（通常需要 LLM）；实现复杂，需要多层处理；存储和计算成本较高   |
| 适合   | 大型文档集合（论文库、法规库、知识库）；需要跨章节推理的复杂问答；文档摘要和导航场景 |
| 不适合 | 短文档；实时性要求高的场景；简单的事实检索                                           |

示例代码：
https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/SubDocumentChunkingStrategy.java
执行结果：![SubDocumentChunkingStrategy.png](imgs/chunking/SubDocumentChunkingStrategy.png)

### 基于文档结构分块

基于文档结构分块利用文档自身的结构标记（标题、章节、页码、表格边界等）进行切分。这是一种"尊重原文"的分块策略。


| 文档类型  | 结构标记                        | 分块方式         |
| --------- | ------------------------------- | ---------------- |
| Word/DOCX | 标题样式（Heading 1/2/3）、段落 | 按标题层级切分   |
| PDF       | 书签、页眉页脚、文本块位置      | 按页或按书签切分 |
| Markdown  | #、##、### 标题标记             | 按标题层级切分   |
| HTML      | h1/h2/h3、section、article      | 按标签结构切分   |
| LaTeX     | \section、\subsection           | 按命令层级切分   |


| 维度   | 说明                                                                                              |
| ------ | ------------------------------------------------------------------------------------------------- |
| 优点   | 贴合文档逻辑结构，信息组织性强；分块边界自然，语义完整；保留文档的层次关系，便于导航              |
| 缺点   | 依赖高质量文档解析，解析失败则效果大打折扣；不同文档格式需要不同的解析器；扫描版 PDF 难以提取结构 |
| 适合   | 结构化文档（正式报告、论文、书籍）；有明确章节层次的文档；需要保留文档导航结构的场景              |
| 不适合 | 非结构化文本（纯文本、聊天记录）；扫描版/图片版文档；格式混乱或结构不清晰的文档                   |

我在 Markdown 文档上最喜欢用这种策略。Markdown 的标题结构天然就是分块边界，按 `#` `##` `###` 切，基本不会破坏语义。
示例代码：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/DocumentBasedChunkingStrategy.java
执行结果：![DocumentBasedChunkingStrategy.png](imgs/chunking/DocumentBasedChunkingStrategy.png)

### 智能体分块（LLM-based / Agentic Chunking）

智能体分块是一种由大语言模型动态决定如何切分的策略。它不依赖固定的规则，而是让模型"理解"文本后做出分块决策。

传统分块：按字数/标点/规则切分，模型不理解内容
智能体分块：让模型阅读文本，判断"哪里该切"

实现方式有三种：

**直接分块法**：将文本交给 LLM，让它直接输出分块边界
**主题识别法**：先让 LLM 识别文本中的主题边界，再据此切分
**任务驱动法**：根据具体任务（问答、摘要、翻译）决定分块策略


| 维度   | 说明                                                                             |
| ------ | -------------------------------------------------------------------------------- |
| 优点   | 灵活性和针对性最强；能处理复杂的语义边界；可根据任务动态调整；分块质量理论上最高 |
| 缺点   | 实现复杂，目前不普及；计算成本高（需要调用 LLM）；延迟大，不适合实时场景         |
| 适合   | 对分块质量要求极高的场景；复杂文档（多主题交织、非线性结构）；研究和实验性项目   |
| 不适合 | 大规模文档处理（成本过高）；实时性要求高的场景；预算有限的场景                   |

目前阶段这个策略更多是探索性的，生产环境用得还不多。但随着模型速度提升和成本下降，Agentic 分块可能成为主流。

### 混合分块

混合分块是结合多种分块策略的"组合拳"。它旨在综合各种策略的优势，在效率、成本和质量之间取得平衡。

常见的混合策略组合：


| 组合策略        | 第一层处理           | 第二层处理                 | 适用场景                 |
| --------------- | -------------------- | -------------------------- | ------------------------ |
| 结构 + 语义     | 先按文档结构切分大段 | 对每个大段进行语义分块     | 长文档精细化处理         |
| 递归 + 语义     | 先用递归分块初步切分 | 对边界处进行语义相似度检查 | 提升分块质量同时控制成本 |
| 固定 + 智能体   | 先用固定大小快速分块 | 用 LLM 检查并调整边界      | 快速+精确的平衡          |
| 模式特定 + 增强 | 按内容类型分块       | 为每个块添加上下文元数据   | 复杂文档高质量检索       |


| 维度     | 说明                                                                         |
| -------- | ---------------------------------------------------------------------------- |
| 优点     | 综合多种策略优势，兼顾效率与语义完整性；灵活可调，可根据场景定制             |
| 缺点     | 实现复杂度较高；参数调优复杂；调试困难，问题定位复杂                         |
| 适合     | 生产环境的大规模 RAG 系统；对质量有要求但成本也需要控制的场景                |
| 不适合   | 快速原型验证；简单场景（单一策略足够）                                       |
| 设计建议 | 从简单策略开始，根据实际需求逐步增加复杂度；建立评估指标，用数据驱动策略选择 |

示例代码：https://github.com/tyronczt/hello-ai/blob/main/rag/src/main/java/cn/tyron/llm/chunking/strategy/HybridChunkingStrategy.java
执行结果：此处使用的是 RECURSIVE_SEMANTIC（递归分块 + 语义边界检查），可按需进行调整类型。
![HybridChunkingStrategy.png](imgs/chunking/HybridChunkingStrategy.png)

## 我的经验总结

分块这件事，没有一招鲜的万能策略。我自己的经验是：

**先用递归分块跑通全链路**。这是最安全、最通用的默认选择，能 cover 住大多数场景。先让系统跑起来，看到效果，建立评估体系，然后再根据具体问题换策略。

**根据文档类型选择基础策略**。Markdown 按标题切，代码按函数切，表格按行切，PDF 尽量先尝试按段落切——不要用一种策略处理所有类型的文档。

**overlap 不要省**。省这一点点存储成本，换来的是检索质量的提升，值。

**块大小不是越小越好**。我见过有人把 chunkSize 设到 50 token，觉得"越精准越好"，结果上下文碎片化严重，模型根本拼不出完整的语义。一般 500 到 1000 个 token 是比较舒服的区间。

**元数据能抽就抽**。来源文档、页码、标题、版本号，这些信息在后面溯源和过滤的时候非常有用。分块的时候顺手带上，不费多少事。

**用真实问题来验证分块策略**。你设的参数好不好，不是你拍脑袋决定的，是用户问出问题来、系统答得对不对来验证的。建立一批测试问题集，每次调整分块策略之后跑一遍看效果变化。

## 写在最后

分块是 RAG 链路里看起来最不性感、但实际上影响最大的环节之一。

模型选对了、Embedding 调好了、Prompt 设计得很完美——但分块切得不对，前面的努力全白费。这件事没有捷径，只能根据你的实际文档、实际用户问题反复调。

我的建议是：**先跑起来，再调优**。不要在一开始就追求完美的分块策略，先用递归分块让整个链路跑通，然后一步一步优化。这个顺序比什么都重要。

下一节我们来聊向量嵌入——文本是怎么变成数字的，以及 embedding 模型该怎么选。
